services:
  game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hexbound-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      - redis
    environment:
      - NODE_ENV=production
    env_file:
      - ./.env.prod
