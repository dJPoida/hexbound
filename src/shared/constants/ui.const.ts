// UI-related enums and constants shared across the application

// Button variants used throughout the application
export enum ButtonVariant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  GREEN = 'green',
  RED = 'red',
  PURPLE = 'purple',
  ICON = 'icon',
  LINK = 'link',
}

// Input types for form elements
export enum InputType {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
  NUMBER = 'number',
  SEARCH = 'search',
  TEL = 'tel',
  URL = 'url',
}

// Button types for HTML button elements
export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

// Typography variants for Text and Heading components
export enum TypographyVariant {
  PAGE_TITLE = 'pageTitle',
  SECTION_HEADER = 'sectionHeader',
  SUB_SECTION_HEADER = 'subSectionHeader',
  LABEL = 'label',
  CAPTION = 'caption',
}

// Text colors for Typography components
export enum TextColor {
  DEFAULT = 'default',
  SUBTLE = 'subtle',
}

// Heading levels for semantic HTML structure
export enum HeadingLevel {
  H1 = 1,
  H2 = 2,
  H3 = 3,
  H4 = 4,
  H5 = 5,
  H6 = 6,
}

// Component sizes
export enum ComponentSize {
  DEFAULT = 'default',
  LARGE = 'large',
  SMALL = 'small',
}

// Input variants
export enum InputVariant {
  DEFAULT = 'default',
  LARGE = 'large',
}

// Checkbox types for HTML input elements
export enum CheckboxType {
  CHECKBOX = 'checkbox',
} 